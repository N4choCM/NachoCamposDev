[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback lives in public/_redirects (copied to dist on build).
# Do NOT add a catch-all redirect here — it breaks netlify dev + Vite module loading.

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  autoLaunch = false
